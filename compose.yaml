services:
  queuestorm:
    build: .
    image: queuestorm:latest
    container_name: queuestorm
    restart: unless-stopped
    env_file:
      - .env.local
    environment:
      NODE_ENV: production
      PORT: 3001
      USE_LLM: ${USE_LLM:-false}
    ports:
      - "3001:3001"
